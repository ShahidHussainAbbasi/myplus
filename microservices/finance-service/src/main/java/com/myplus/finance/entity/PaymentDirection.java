package com.myplus.finance.entity;

/** RECEIPT = money in (AR, customer pays us). DISBURSEMENT = money out (AP, we pay a vendor — Phase 2). */
public enum PaymentDirection {
    RECEIPT, DISBURSEMENT
}
