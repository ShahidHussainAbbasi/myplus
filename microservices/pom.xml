<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.myplus</groupId>
  <artifactId>myplus-microservices</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.0</version>
    <relativePath/>
  </parent>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <spring-cloud.version>2025.0.1</spring-cloud.version>
    <jjwt.version>0.12.6</jjwt.version>
    <lombok.version>1.18.46</lombok.version>
    <modelmapper.version>3.2.0</modelmapper.version>
    <poi.version>5.4.0</poi.version>
    <mysql.version>9.1.0</mysql.version>
    <!-- OpenTelemetry Spring Boot starter (traces + logs export via OTLP), non-agent. Bump to the
         latest tag compatible with this Spring Boot line if the build complains:
         https://github.com/open-telemetry/opentelemetry-java-instrumentation/releases -->
    <opentelemetry-instrumentation.version>2.11.0</opentelemetry-instrumentation.version>

    <!-- See the maven-surefire-plugin comment below: without this every container test skips silently. -->
    <docker.api.version>1.41</docker.api.version>
  </properties>

  <modules>
    <module>eureka-server</module>
    <module>config-server</module>
    <module>common-security</module>
    <module>common-service</module>
    <module>common-web</module>
    <module>common-settings</module>
    <module>common-outbox</module>
    <module>common-subledger</module>
    <module>common-credit</module>
    <module>common-installment</module>
    <module>common-import</module>
    <module>commerce-domain</module>
    <module>commerce-contracts</module>
    <module>commerce-pricing</module>
    <module>common-notify</module>
    <module>common-captcha</module>
    <module>service-parent</module>
    <module>api-gateway</module>
    <module>auth-service</module>
    <module>catalog-service</module>
    <module>inventory-service</module>
    <module>business-service</module>
    <module>finance-service</module>
    <module>audit-service</module>
    <module>party-service</module>
    <module>education-service</module>
    <module>welfare-service</module>
    <module>agriculture-service</module>
    <module>pharma-service</module>
    <module>marketplace-service</module>
    <module>notification-service</module>
    <module>campaign-service</module>
    <module>analytics-service</module>
    <module>appointment-service</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.opentelemetry.instrumentation</groupId>
        <artifactId>opentelemetry-instrumentation-bom</artifactId>
        <version>${opentelemetry-instrumentation.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>${jjwt.version}</version>
      </dependency>
      <dependency>
        <groupId>org.modelmapper</groupId>
        <artifactId>modelmapper</artifactId>
        <version>${modelmapper.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.poi</groupId>
        <artifactId>poi-ooxml</artifactId>
        <version>${poi.version}</version>
      </dependency>
      <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <version>${mysql.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jboss.aerogear</groupId>
        <artifactId>aerogear-otp-java</artifactId>
        <version>1.0.0</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <annotationProcessorPaths>
              <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
              </path>
            </annotationProcessorPaths>
          </configuration>
        </plugin>
        <!--
          Testcontainers: pin the Docker API version docker-java negotiates with.

          WITHOUT THIS, EVERY @Testcontainers TEST SILENTLY SKIPS ON WINDOWS AND THE BUILD STILL SAYS SUCCESS.
          docker-java as bundled in Testcontainers 1.21.0 falls back to API 1.32 when it cannot negotiate.
          Docker Engine 29.x advertises "minimum version 1.40", so it answers 1.32 with HTTP 400 and a stub
          /info payload; Testcontainers reads that as "no valid Docker environment" and disables every test
          guarded by disabledWithoutDocker. The summary line then reads
          "Tests run: 157, Failures: 0, Skipped: 13" — and a skipped test is indistinguishable from a passing
          one at a glance. On this repo those 13 covered the migrations, invoice money arithmetic, the sale's
          atomicity and multi-tenant scoping: the riskiest ground in the module.

          ⚠ ONLY THE SYSTEM PROPERTY WORKS. Measured 2026-08-22, all three:
            api.version=1.41 in ~/.testcontainers.properties  -> still skipped (not forwarded to docker-java)
            DOCKER_API_VERSION=1.41 environment variable      -> still skipped
            -Dapi.version=1.41 system property                -> 0 skipped

          Harmless on CI, where negotiation already works: 1.41 sits inside the engine's supported range
          (min 1.40, max 1.55). Override with -Ddocker.api.version=... if an engine ever raises its floor
          past this — which fails LOUDLY, unlike the silent skipping this prevents.
        -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <configuration>
            <systemPropertyVariables>
              <api.version>${docker.api.version}</api.version>
              <!-- No OTLP collector exists during a test run, so the SDK falls back to its own
                   localhost:4318 default (the config-server yaml is never fetched - tests set
                   spring.cloud.config.enabled=false) and logs a ConnectException per export attempt,
                   with a retry interceptor still flushing at shutdown. Disabled here rather than in
                   each test class: 20 Testcontainers classes across 9 modules would each need the
                   line, and a new one could forget it. Surefire-only, so running services are
                   unaffected. Nothing under test exercises OpenTelemetry. -->
              <otel.sdk.disabled>true</otel.sdk.disabled>
            </systemPropertyVariables>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <configuration>
            <excludes>
              <exclude>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
              </exclude>
            </excludes>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
