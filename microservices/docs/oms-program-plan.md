# OMS / Platform Program — execution tracker

**Status:** LIVE TRACKER - the status column in each table is the source of truth for OMS/platform work. Update the row as a slice lands, not this line.

Live checklist for implementing [`platform-oms-master-reference.md`](platform-oms-master-reference.md). One slice at a
time: **Document → Design (Mermaid) → Implement → headed Cypress gate** (you run the gate; your pass closes the slice).
Branch **`feature/oms`**. Update the status column as slices land.

**Legend:** ⬜ not started · 🟡 in progress · 🟢 design approved, implementing · ✅ gate green.

## Main line — OMS (repair-in-place → extract → channels)

| # | Slice | Fixes / adds | Design doc | Gate | Status |
|---|---|---|---|---|---|
| **O1** | Storefront → books | OMS-1, OMS-5 | [oms-O1-storefront-to-books.md](slices/oms-O1-storefront-to-books.md) | `order-to-ledger.cy.js` | ✅ **GREEN 2026-08-06 — 10/10.** Storefront orders now go through business-service's ONE sale path: invoice + GL + tax + AR, server-priced (OMS-5), idempotent per cart. Cancel VOIDS the invoice (stock back **and** books reversed) via `SaleVoidService`, extracted so `/voidSell` and `/internal/sales/reverse` share one reversal. Duplicate marketplace saga + `OrderSagaRecoveryRelay` deleted. Built on `feature/b2b-b2c`. Reconciliation read `GET /orders/reconciliation` (owner/admin) lists the pre-O1 backlog. **Open:** CARD tender not yet passed into the sale, so a paid card order reads as AR until the PSP slice. |
| **O2** | Lifecycle + authority + safety | OMS-2/3/4/8 | [oms-O2-lifecycle-authority-safety.md](slices/oms-O2-lifecycle-authority-safety.md) | `order-lifecycle.cy.js` | ✅ **GREEN 2026-08-06 — 7/7** (+9/9 unit, 31/31 regression). Transition whitelist (`SHIPPED → CANCELLED` deliberately illegal — the O1 void would return stock still on a van); status change gated, admin for the reversals; order placement idempotent on the SAME key the sale uses, with a duplicate-key catch for the concurrent race; `@Version` on `Order`; per-org `SO-` numbers (Flyway `V11` + backfill) and public tracking resolved by number, not a walkable auto-increment id. **This clears the last P0 in the gap register.** Open: OMS-7 unbounded reads (O4); lock conflict surfaces as 500 rather than 409. |
| **O3** | `order.*` per-org config | 🔒 config | [oms-O3-order-config.md](slices/oms-O3-order-config.md) | `order-config.cy.js` | ✅ **GREEN 2026-08-06 — 9/9** (+67 unit, 25/25 regression). Delivery fees, free-over threshold and COD are per-org, resolved by ONE `ShippingPolicy` used by both `quote()` and `place()`; COD refusal enforced server-side and no longer *offered* (the quote carries `codEnabled`). `getDecimal` moved into `common-settings` with 4b switched onto it. **Two defects the gate caught before ship, both silent-by-construction:** (1) the policy resolved its tenant from the JWT, but the storefront is anonymous — so per-org fees applied to staff orders and to no real customer; the org is now a **required parameter** (`effectiveFor(org, …)`). (2) marketplace had **no `SettingsStore`**, so the `@ConditionalOnBean` engine never started, and `@Autowired(required=false)` turned that into "everyone keeps the default fee" instead of a boot failure — store added, injection now **required**. Also fixed platform-wide: all four Configuration screens wrote `el.checked` for every control, so any SELECT/INT/TEXT/MONEY setting saved as `"false"`. |
| **O4** | Back-office UI/UX | OMS-7 | [oms-O4-back-office.md](slices/oms-O4-back-office.md) | `order-back-office.cy.js` | ✅ **GREEN 2026-08-06 — 15/15** (+84 unit, 52/52 across 9 specs). **OMS-7 closed:** the list is paged and filtered IN THE QUERY with a server-enforced cap (`?size=100000` clamps to 100), behind a new `(organization_id, created_at)` index — pagination without it would only have moved the cost into the database. `allowedTransitions` is published on the DTO and the browser's rival copy of the lifecycle rules (`NEXT`) is deleted, which removes the phantom Cancel on a SHIPPED order (server answered 409) and makes `RETURN_REQUESTED` actionable — one cause, both symptoms. Detail now shows lines, money, refund state and the `order_events` timeline that had been written since slice 46 and shown only to the SHOPPER. Refund is reachable at last (built in slice 70, never wired to a screen). Also added: `/getOrder` proxy (the service endpoint had existed since slice 46 with no monolith route) and `V14 order_items.product_name`, snapshotted at write because the cart already carried it and checkout discarded it. Stale specs retired: `order-saga-relay` DELETED (asserted a saga O1 removed; **nothing writes `reservationStatus` any more**), `ecommerce-orders` FIXED (asserted the NEW→SHIPPED jump O2 made illegal). ~~Backlog: `reservationId`/`reservationStatus` are dead columns.~~ **CORRECTED 2026-08-10 — half of that was wrong, and acting on it would have broken live behaviour.** `reservationId` is **NOT dead**: it is read in three places (`updateStatus`'s cancel guard, `processReturn`'s guard, and `returnStockQuietly`, which passes it to `inventoryClient.returnStock`) and is **the only way a pre-O1 order — which has no invoice — can have its stock returned at all**. Dropping it would silently turn cancel and return into no-ops for every legacy order. Only `reservationStatus` is genuinely written nowhere; it is read solely to copy into the DTO. Textbook **D5** — *never drop on inference; the entity-vs-usage diff is a starting point, not evidence*. If `reservationStatus` is ever dropped it goes alone, and the DTO field goes with it. <br>_(prior: design ready 2026-08-06 — awaiting approval.)_ Verified beyond OMS-7's unbounded `findScoped`: there is **no index** behind the list (`(organization_id)` only, sorted by `created_at`); the UI keeps a **second copy of the transition rules** (`ecommerce.js` `NEXT`) which has **already drifted from O2** — Cancel is offered on a SHIPPED order the server refuses with 409, and `RETURN_REQUESTED` is inert; and **refund + return are built, admin-gated, error-relaying, and unreachable from the UI** (slices 70/71 built the hard half). Fix: paginated/filtered read with a server-enforced size cap + `V13` index; `allowedTransitions` on the DTO so the client stops guessing (`NEXT` deleted); detail with lines, money and the `order_events` timeline that has been written since slice 46 and never shown. |
| **O5a** | Reservation expiry | OMS-6 | [oms-O5a-reservation-expiry.md](slices/oms-O5a-reservation-expiry.md) | `reservation-expiry.cy.js` | ✅ **GREEN 2026-08-07 — 8/8** (+35 unit, 97/97 regression across 9 specs). **O5 was split: this is the DEFECT, O5b is the features.** Holds now carry `expires_at`; a scheduled sweeper returns anything past it as `EXPIRED` (distinct from `RELEASED` — "nobody came back" is the fact worth investigating). The sweeper **re-reads under a write lock** so it cannot race a confirm into an oversell, is bounded, and is idempotent. `confirm` stays lenient until a hold is actually swept, because an unswept hold still physically holds its stock. TTL is per-tenant (`inventory.reservation.holdMinutes`, 0 = never) and read with `effectiveFor(orgId,…)` since the sweeper has no security context. `held` is now published beside `onHand`/`sellable`/`expired`, and the uncompensated leak in `SellController`'s sale-edit path is closed. Gateway needed a stripped `inventory-settings` route — the shared settings controller sits at `/settings` and inventory has no StripPrefix. <br>_(prior: design ready 2026-08-06 — awaiting approval.)_ Verified: `Reservation` has no `expiresAt` and there is no sweeper anywhere, yet `SagaSellService` logs *"held stock will lapse/cleanup later"* — the code documents a mechanism that was never built, which is why the gap survived review. Consequence is not a delayed sale: `available = quantity − reservedQuantity`, so a stranded hold makes stock **permanently unsellable** while still counted in on-hand. **Two leak sites** — the saga at least attempts a release; `SellController:891-897` (sale edit) reserves then confirms with **no try/catch and no release at all**. And it is invisible: `getLevelDetail` publishes `{onHand, sellable, expired}` and silently absorbs holds into a lower `sellable`. Fix: `expires_at` + `EXPIRED` status, a locked-re-read sweeper (must not race a confirm into an oversell), per-tenant `inventory.reservation.holdMinutes` (0 = never), `held` published next to `sellable`, and the uncompensated site closed. |
| **O5b** | Shipments (partial + split + carrier) | order is all-or-nothing | [oms-O5b-shipments.md](slices/oms-O5b-shipments.md) | `order-fulfilment.cy.js` | ✅ **GREEN 2026-08-07 — 12/12** (+105 unit, 105/105 regression across 13 specs). An order ships in parts: `shipment`/`shipment_line` + `order_items.quantity_shipped`, per-org `SHP-` series, carrier + tracking visible to the shopper. **Shipping progress is DERIVED** — `PUT /status` refuses `SHIPPED`/`PARTIALLY_SHIPPED` and names the shipment endpoint, and `AllowedTransitionsTest` asserts no state offers a derived status as a manual move, so the old button cannot return. `PARTIALLY_SHIPPED → CANCELLED` is illegal alongside `SHIPPED → CANCELLED`. **§2.4 of the approved design was WRONG and was withdrawn**: stock leaves at the SALE (O1), not at dispatch, so reversing `quantityShipped` would have invented a shortfall — `processReturn` is unchanged. **The gate also found a live O2 defect: public tracking threw for every tenant once two orgs existed**, because `findByOrderNo` was global while `order_no` is unique only per org. <br>_(prior: design ready 2026-08-07 — awaiting approval.)_ Verified: `OrderItem` records `quantity` only, so *3 of 5 shipped* is unrepresentable; "shipped" is a claim a packer types, with nothing recording what left, when or with what tracking; `ShippingOption` still comments that carrier/tracking is future work. The turn of the slice: **shipping progress becomes DERIVED from line quantities** (you record a shipment, the status follows) while `DELIVERED`/`CANCELLED`/`RETURNED` stay explicit decisions — a status settable independently of its shipments is one that can lie about them, which is the duplication O4 just removed. Also forced: `returnStockQuietly` and `processReturn` reverse the ORDERED quantity, which is correct only while nothing can be partly shipped — so `PARTIALLY_SHIPPED → CANCELLED` joins `SHIPPED → CANCELLED` as illegal, and returns reverse what actually shipped. **A shipment moves no stock** — O1 decremented at sale time. |
| **O5c** | Backorders + promise dates | order is lost when stock is short | [oms-O5c-backorders.md](slices/oms-O5c-backorders.md) | `order-backorder.cy.js` | ✅ **GATE GREEN 2026-08-09 — 10/10** (+121 unit). A shop can accept what it cannot fill today: only the fillable part is invoiced, the shortfall lives on the ORDER (never in inventory, so stock cannot go negative), and a TOTAL shortfall raises **no sale, no invoice and no card charge** until goods move — `booksStatus = BACKORDER_PENDING`, kept distinct from `LEGACY_UNPOSTED` so a healthy unbooked order never hides inside a real reconciliation backlog. Three layered settings (`allowed` → `acceptFullShortfall` → `promiseDays`), off by default. **§2's risky change was avoided**: splitting the ORDER before requesting the sale leaves `SagaSellService` untouched, because the request is then fully satisfiable. **§3.3's sweeper was dropped** — readiness is derived from stock that already exists, so a read answers it and a stored flag would only go stale (O5a needed a job because it had to MUTATE; this does not). **The gate found the storefront refusing orders the API accepted in THREE places** (disabled Add, "Out of stock" label, and `addToCart`'s stock cap) — adding a capability means re-examining the existing refusals. <br>_(prior: design ready 2026-08-07 — awaiting approval.)_ Today insufficient stock **rejects the sale outright**: a shopper who wants 10 and can have 8 gets nothing. O5b already built the mechanism to resolve it (ship in parts); what is missing is permission to ACCEPT the order. **§2 is the decision to review — it changes the O1 revenue path.** A backorder breaks O1's assumption that the sale is recorded whole at placement: recommended option A invoices only the AVAILABLE quantity and carries the shortfall on the ORDER, uninvoiced, so no negative stock and no revenue recognised for undelivered goods. Requires the sale path to accept a PARTIAL reservation, which is deliberately all-or-nothing today. Off by default. Resolution via an O5a-style sweeper (no event broker exists), which flags "ready to fulfil" but never auto-allocates — taking goods for an old order ahead of a customer at the till is a merchant's decision. |
| **O5e** | POS order parity | **OMS-5** (the last open original defect) | [oms-O5e-pos-order-parity.md](slices/oms-O5e-pos-order-parity.md) | `pos-order-parity.cy.js` | ✅ **GREEN 2026-08-10 — all four steps, in §2.3's mandated order. OMS-5 CLOSED, and with it every one of the eight original OMS defects.** A POS sale now produces its order **server-side**, carrying the sale's lines, the sale's total, an `SO-` number and idempotency; **cancelling a POS order restores stock** (impossible before — the guard is `!items.isEmpty()`); closing the browser loses nothing. Step 4 deleted the browser writer (`ecommerce.js` `recordOrder` + its `main.js` call site) only after step 3 gated green, so the two-writer window was never unkeyed. **Option C — the MONOLITH orchestrates**, not business-service: giving business-service a marketplace client would have made the pair mutually dependent against O1's direction, so `PosOrderRecorder` (`com.web.util`) runs after `SellController.addSell` returns — gate → authoritative read → best-effort POST, never throwing, because the money is already committed. Accepted trade: a monolith crash between sale and order still loses an order (an outbox fixes it once Track C has a broker). **§2.5's own load-bearing claim was FALSE** — it said the lines and total were already in `addSell`'s response, which returns `GenericResponse("SUCCESS", msg, invoiceNo)`, the invoice number and nothing else; step 3 therefore reads the invoice back via `/getReceipt`. Implemented as written it would have recomputed the total client-side, i.e. re-opened gap B, the exact defect the slice existed to close. Vertical gate is `ModuleRouter.moduleOf` (activeOrgType→userType), **not** `userType` alone the way `CommerceDashboardController` does it — and note the gate could not have settled that, since the fixture resolves to MARKETPLACE under both rules. Also: `record()` now stamps `booksStatus=POSTED` when the order names an invoice. <br>_(prior: approved 2026-08-09; steps 1–2 green, step 3 blocked on §2.5's dependency-inversion decision.)_ Verified WIDER than the register said: `record()` persists **no line items** (so a POS order can never restore stock on cancel/return), takes a **client-computed total**, and has **no `orderNo` and no idempotency key** — O1's and O2's hardening reached `placePublic` only. Fix is O1 in reverse: the SALE creates the order server-side, so the browser reports neither. ⚠️ **§2.3 sequencing is mandatory** — idempotency on `invoiceNo` ships FIRST and alone; landing it after the server-side call means two writers with no key between them and duplicate orders per sale. |
| **O5d** | Pick / pack workbench | packing is untooled | [oms-O5d-pick-pack.md](slices/oms-O5d-pick-pack.md) | `order-pickpack.cy.js` | 🟡 **HALF BUILT — backend DEPLOYED (with O5e, 2026-08-09) but STILL NOT GATED. This is the OMS main line's only unfinished slice.** In: two settings, `V17 shipment_line.verified`, and server-side enforcement of `scanRequired` in `ShipmentService` (the only writer, so the endpoint cannot be bypassed). **Out: the entire packer-facing half** — pick-list read, scan resolution, workbench UI, `PackVerificationTest`, `order-pickpack.cy.js`. ✅ **Both packing settings WITHDRAWN 2026-08-10** (see §2.4c): `autoConfirm` was read nowhere in `main/` (inert, as `pharmacy.interaction.blockSevere` was), and `scanRequired` was worse than inert — **a trap**: enforced correctly in `ShipmentService`, but `submitShipment` posts `{orderItemId, quantity}` and never `verified`, so switching it on refused **every** dispatch and told the packer to scan into a workbench that does not exist. Catalog entries removed, and the enforcement removed with them — leaving it dormant behind a withdrawn key would strand any tenant who had already enabled it with no screen to turn it off. `V17`, `ShipmentLine.verified` and the per-line recording all stay; they are what the workbench writes into. ⚠️ **Third occurrence of one pattern** — O3 shipped a setting nothing read, O4 shipped endpoints nothing could reach, O5d shipped a policy no UI could satisfy: **a capability is not shippable until something can exercise it.** <br>_(prior: design ready 2026-08-09.)_ O5b made dispatch *recordable*; it did not make it *workable*. A packer still reads quantities off a screen and types them back, so the one error that matters — **packing the wrong thing** — is undetectable. Reuses the barcode lookup the POS already has (`/products/lookup`) rather than building a second scanner: scan-to-pack verifies against the order instead of trusting typing. No new money or stock path — it produces the same `ShipmentDTO.Request` the Ship action already posts, so O5b's guards remain the only writer. |
| **INV-L** | **Stock by location** | multi-store share ONE pool | _tbd_ | `stock-by-location.cy.js` | ⬜ **Moved OUT of OMS.** Verified: inventory has NO location concept — no `storeId`/`locationId` on `StockEntry`, `StockLevel` or `Reservation`, and `StockReservationRequest` carries none — although multi-store/multi-branch shipped long ago. This is a foundational inventory change (every entry, reserve, FEFO pick and level read), not order routing, so it belongs with the multi-location programme rather than behind an order feature. |
| **O6** | Extract `order-service` (:8097) | service split + dual-read | _tbd_ | all prior specs green | ⬜ |
| **O7** | **Distribution pre-sales** (booker → validation → pack → deliver → collect) | the FOUNDING requirement | [oms-O7-distribution-presales.md](slices/oms-O7-distribution-presales.md) | per-phase D1–D6 | 🟢 **D1 GREEN 2026-08-11** (12 cases) · **D2 GREEN 2026-08-12** (`order-booker.cy.js`, 8 cases): `ROLE_ORDER_BOOKER` (which **withholds** — no ADMIN_PRIVILEGE, so a rep gets a 403 confirming their own order rather than a hidden button), `booker.marketplace@` seeded INSIDE the marketplace owner's org, V19 attribution (`booked_by_user_id` + a **stamped** `booked_by_name`, because an order outlives its staff), `?mine=true` as a **boolean the server resolves** (not `bookedBy=<id>`, which a rep could point at a colleague), and `/creditStanding` so an over-limit outlet is caught **at the counter** instead of by the warehouse a day later. **DRY call:** `creditAccountOf`/`groupExposure` were private to `SagaSellService`; copying them would have had the booker told one thing and the sale enforcing another, so they moved to `CreditStandingService` and the sale path delegates. ⚠️ **D2 ships no booking SCREEN** (split out) and **`ROLE_ORDER_BOOKER` is NOT a territory confinement** — `?mine=true` is a filter the rep chooses, not a wall. <br>_(D1 detail:)_ Booked orders now have a review phase: `PENDING_APPROVAL`/`REJECTED` in front of the existing lifecycle, `PUT /orders/{id}` amendment with a stamped audit trail, confirm/reject with a mandatory reason, and **`ON_DISPATCH` invoicing** via `DispatchInvoiceService` (its own class — `OrderService` already depends on `ShipmentService`, so putting it there would make the two mutually dependent). `PUT /status` refuses the approval moves and names the proper endpoint, so the generic route cannot bypass the confirm gate. Six monolith proxies ship with it — an endpoint with no proxy is exactly what R7 caught three times. ⚠️ **Two deliberate departures, in §8.1:** stock is **not** reserved at confirm (that would re-add the marketplace reservation saga **O1 deleted**), and the margin/credit re-checks run at dispatch rather than at amend time. Both need one new trade-contract operation = **D1b**. **Five defects during implementation, four found by reading and one by the gate — and the gate's was the worst:** `invoiceForDispatch` guarded on `booksStatus`, which `ShipmentService` flips to `POSTED` after the first parcel, so **every subsequent parcel raised NO invoice and its goods left with nothing behind them** — OMS-1, on the partial-delivery path that is a distributor's normal week. Root cause worth carrying: **a guard keyed on a STATE where it needed a PROPERTY** (now `source == FIELD`, stamped once at booking). Invisible to four passes of reading because each file was correct alone — the defect lived in the ordering of two lines in two files. <br>_(prior: design approved 2026-08-11; all five decisions answered.)_ **The five decisions:** Decisions: **`ON_DISPATCH`** invoicing with stock reserved at CONFIRM (reserve early, invoice late); **both** booker and admin may revise a rejection (⇒ concurrent-edit 409 + a mandatory amendment audit trail — with two editors, *"who dropped the price?"* is otherwise unanswerable); admin **may** change prices but **inside the existing `pos.sale.marginPolicy`**, not above it, and an amendment **re-runs both the margin and credit checks** (an order that passed at booking is not still passing after it is edited); **one** approval step, modelled so a second can be inserted later; **Ahsan has NO device** ⇒ D4 becomes a delivery-return keying screen for the admin, the signed paper invoice is the POD, entries attributed as *keyed from a signed invoice* rather than as the driver's confirmation, and **D5 driver settlement becomes the only real control on the cash**. <br>_(prior: design written 2026-08-10.)_ The process is textbook **pre-sales DSD** and needs no arguing with; **the second half is built and the first half does not exist.** Verified full gaps: no order-booker role, no `bookedBy` on `Order`, **no approval state** (`NEW` already means "ready to pack"), and **no order-amendment endpoint of any kind** — so Javed's review step, the pivot of the whole process, is unimplementable today. Good foundations to reuse: `receivePayment` (FIFO AR, ledger-posting, idempotent), `Shipment`/`SHP-`, the credit engine, role×location grants. **§2.1 is the decision that gates everything: a booked order must NOT invoice** — otherwise an amendment edits an issued invoice and a rejection voids one. Recommends `invoiceTrigger = ON_DISPATCH`, which settles programme Open Decision #2. Also pushes back on two points of the stated process: no manual "Dispatched" button (O5b made it derived, deliberately), and **delivery must be confirmed by the person who delivered it**, not retyped by the admin. Phased D1–D6; D3 finishes O5d and restores its withdrawn settings. |
| **O8** | Remaining channels + vertical adapters + CQRS read-model | procurement PO/GRN, pharma dispense, appointment, education/welfare | _tbd_ | per-vertical | ⬜ |

## End-to-end review, 2026-08-10 — findings against the governing standards

A full pass over the OMS surface (marketplace-service + the monolith's order/sale path) against
[`SAAS-BUILD-STANDARDS.md`](SAAS-BUILD-STANDARDS.md). **Eight findings; six fixed, two left open deliberately.**

✅ **RE-GATED GREEN 2026-08-10.** `order-backorder` (incl. the new `?size=100000` clamp case and the moved
`data.content` shape) · `order-fulfilment` · `ecommerce-orders` · `order-cancel` · `order-back-office` ·
`pos-order-parity`, plus `mvn -pl marketplace-service test`. **This run is also what confirms O5e step 4** —
the browser writer was removed after the previous green, so `pos-order-parity` had not yet been observed
against the single-writer path until now.

| # | Finding | Standard | State |
|---|---|---|---|
| **R1** | **Both O5d packing settings were live and unusable.** `order.pack.autoConfirm` read nowhere in `main/`; `order.pack.scanRequired` enforced but unsatisfiable, because `submitShipment` never posts `verified` — switching it on refused **every** dispatch. | **C1** | ✅ **Fixed** — both withdrawn from the catalog, the enforcement removed with them (leaving it dormant behind a withdrawn key would strand any tenant who had already enabled it, with no screen left to turn it off). See O5d §2.4c. |
| **R2** | **An optimistic-lock conflict returned `500 Internal server error`.** O2 added `@Version` to `Order` and the failure fell through to the catch-all — so the guard worked and then told the caller "we are broken" on the one path where nothing is broken, using the status clients are told never to retry. | §1.5 | ✅ **Fixed** in `common-web` — `OptimisticLockingFailureException` → **409** with "Someone else changed this while you were working on it." Fixes every service using the shared handler, not just marketplace. |
| **R3** | **The shared 500 handler logged nothing and echoed `ex.getMessage()` to the caller.** The only record of an unexpected failure was the sentence handed to the browser, and that sentence carried SQL fragments, constraint names and class names. **D3d one layer up** — the component that knew the answer threw it away. | **D3d** | ✅ **Fixed** — logs at ERROR with the stack trace; the response is now a generic sentence. |
| **R4** | **`OrderService.list()` — the exact unbounded read OMS-7 named — was still public with zero callers.** O4 replaced it for the screen and left it in place, so the next person needing "all orders" would find an uncapped method and reintroduce the defect writing no new SQL. | **OMS-7** | ✅ **Fixed** — deleted, with a note explaining why `findScoped` stays on the repository. |
| **R5** | **`/orders/reconciliation` and `/orders/backorders` were still unpaginated `List` reads** — the same defect class O4 closed on the main list, in two endpoints O4 did not touch. Reconciliation is a fixed historical set, but `?booksStatus=POSTED` points that same query at every order ever booked; **backorders grows with the business**. | **OMS-7** | ✅ **Fixed** — both paged, sharing O4's server-side cap (`OrderQuery.clampSize`, extracted so two endpoints cannot drift to two ceilings). The backorder count query is given explicitly, or the `JOIN o.items` would count a two-line order twice. **Honest limitation recorded in the javadoc:** `ready=true` filters the PAGE, not the query — readiness lives in inventory and cannot be a SQL predicate, and making it exact means walking the whole book, i.e. the unbounded read again. Paging also fixed an unnoticed perf defect: readiness was read for the ENTIRE backlog on every call, now just the page. |
| **R7** | **Neither reads is reachable from any screen.** `/orders/reconciliation` has **no monolith proxy at all**; `/orders/backorders` has a proxy and a Cypress gate but **no UI** (`grep backorder` over `ecommerce.js` returns nothing). O1's row claims reconciliation ✅ and O5c's claims the back office "can see what is owed" — both true only of the API. | §1.6 | ⬜ **Open — deliberately not half-built.** Adding a proxy without a screen would be a fourth inert capability. Each needs a small UI slice with its own gate. Recorded here so the tracker stops implying a merchant can see either. |
| **R8** | **marketplace-service had NO Flyway test — all 17 migrations were unexercised by `mvn test`.** Every existing test runs `flyway.enabled=false` + `ddl-auto=create-drop`, i.e. against a schema Hibernate invents. **Only pharma-service satisfies D2 platform-wide.** The exposure here is specific: `fulfilment_status` is a real MySQL `ENUM` widened by three separate migrations (V7→V15→V16), and a missing constant fails at runtime with *"Data truncated"* — invisible to every test that never runs the migration. | **D2** | ✅ **Fixed for marketplace** — `FlywayMigrationTest`: empty MySQL, Flyway builds the schema, `ddl-auto=validate` forces Hibernate to agree. Asserts the ENUM covers **every Java constant, derived from the enum itself**, so adding one without its `ALTER … MODIFY` fails in CI instead of production. ⬜ **The other 10 services still have no such test** — see Housekeeping. |
| **R6** | **A card-paid storefront order still records no tender**, so it sits in AR as though unpaid. Verified still accurate and still deliberate — `toSaleRequest` documents it, and the card is charged only *after* the sale exists, which is the ordering that keeps an out-of-stock checkout from charging anyone. | §1.5 | ⬜ **Open by design** — owned by **E6 (real PSP + refunds)**. Left deferred rather than fixed here: it is a money path and belongs in the slice that also handles webhooks and money-side refunds. |

**Also corrected:** the O4 row's "`reservationId`/`reservationStatus` are dead columns" backlog note was **half wrong**
— see that row. Acting on it would have broken cancel and return for every pre-O1 order.

**The pattern worth naming, seen three times now:** O3 shipped a setting nothing read, O4 shipped endpoints nothing
could reach, O5d shipped a policy no UI could satisfy. Each was a server half landed on its own, each passed its
unit tests, and none was catchable without an end-to-end check. **A capability is not shippable until something can
exercise it** — which is the same rule the Cypress gate encodes, applied at design time instead of at the end.

## Parallel tracks (interleave independently)

| Track | Scope | Status |
|---|---|---|
| **A — Config/Authz rollout** | `common-settings` → catalog/inventory/finance/pharma/marketplace/appointment; finish `@PreAuthorize` tail | ⬜ |
| **B — B2B commercial** | account hierarchy+roles (party) → contract/tiered pricing + `/price/calculate` (catalog + `commerce-pricing`) → quotes→approval→order → credit limits/terms (finance AR) | 🟡 **B1 + B4 DONE; documents/reports DONE** — `feature/b2b-b2c` Phases 0–3 all Cypress-green (2026-08-01→04). **B1** = contract/tier pricing via the new `commerce-pricing` library (rules never stack; one quote per sale). **B4** = credit limit + payment terms, customer AND supplier, warn = take confirmation. Also landed: CRN-/DBN- return documents, batch traceability IN and OUT, CSV statements, report filters + grouping. **Phase 3 CLOSED 2026-08-04** including **3f** — statements now show credit/debit notes and invoices are no longer retro-edited (an invoice issued at 500 used to read 300 after a return, contradicting the customer's own copy). **Phase 3g CLOSED 2026-08-05** (printable trade invoice + owner-designable documents; renderer and designer both gated). **Phase 4a — the ACCOUNT HIERARCHY — CLOSED 2026-08-05, 8/8 green**: company→branch→contact on `Party` (so education corporate sponsors and welfare corporate donors inherit the structure), shared-pool credit **stamped** on `Customer.creditAccountCustomerId` so the sell path keeps a single local read. `common-credit` needed no change — `CreditLimitPolicy` was already correct pure arithmetic. **Phase 4b CLOSED 2026-08-06, 6/6 green** — `SalesQuote` (own `QTE-` series, `@Version`, derived expiry, internal
approval gate distinct from customer acceptance) converting through the SAME revenue path, customer PO carried to
the printed invoice, and **D-4 settled + implemented**: trade discount posts to 4200 Sales Discount
(contra-revenue), where it had been captured since 3g but never posted. **TRACK B IS COMPLETE.** ⚠️ Note the gate caveat: every slice above passed a *single-spec* gate; the full business suite was not run until 2026-08-04 and needed [slice 106](slices/106-cypress-suite-health.md) before it could be trusted. |
| **C — Platform** | event broker + **CQRS analytics read-model** → hash-chained audit → metrics/SLOs → object storage → API versioning → shared `config-screen.js` | ⬜ |

## Housekeeping / prerequisites
- ⬜ **Verify G1** (expired-FEFO) — `mvn -pl inventory-service -am test -Dtest=ReservationServiceTest` (commit `492601d`, already in history). *Still open 2026-08-10 — it is a one-command check that has been carried for weeks; fold it into the next `mvn test` run rather than tracking it separately.*
- ⬜ **D2 is satisfied by exactly TWO of twelve services.** Before this review only `pharma-service` had a
  `FlywayMigrationTest`; `marketplace-service` now has one (R8). The other ten still build their test schema
  with `ddl-auto`, so **their migrations have never run in CI** — meaning no deploy path is proven for any of
  them, and MySQL-ENUM widenings (the `"Data truncated"` class) are invisible until production. The pattern is
  now written twice and copy-ready; this is a per-service afternoon, not a slice.
- ⬜ **Seven services still carry their own copy of `GlobalExceptionHandler`** (agriculture, welfare, campaign, auth, business, education + the `gen_java.sh` template), byte-identical in the parts that matter to the shared `common-web` one that was supposed to replace them. R2's 409 mapping and R3's logging fix therefore reached **marketplace and the other common-web consumers only**. Consolidating is a DRY win with real blast radius (each copy may carry service-specific handlers), so it wants its own slice — but until then, **a fix to the shared handler is not a platform-wide fix**, which is exactly the assumption that makes a shared library dangerous.
- ✅ Commit in-progress **education report-cards** work on `feature/education-review` (keeps `feature/oms` clean) — done: 1.5 report cards is commit `67365448`, 1.6 promotion `cd3f35dd`. That branch has since finished all of Phase 1, closed the education review, and shipped Phase 2.1 (timetable), 2.2 (substitution) and 2.3 (staff attendance &amp; leave), plus 2.4 (homework) and 2.5 (behaviour log) — **Phase 2 is COMPLETE**, all green by 2026-08-03. Next there is Phase 3 (parent/student portals, gated on D-2 and D-4), with a notification slice the strongest candidate first.

- ✅ **Deploy topology hardened 2026-08-05** — `docker-compose.yml` gained **compose profiles** (default = the 14-service POS subset · `--profile pharmacy` = +pharma · `--profile full` = all 22), replacing the hand-typed runbook service list that had already drifted and killed a deploy ("no such service: party-service"). Every app service gained a **readiness** healthcheck (`/actuator/health/readiness`, not `/actuator/health` — the latter aggregates `db`, which flaps DOWN under load and would take healthy containers with it), and the monolith now waits on `api-gateway: service_healthy`. `start_period` 240s after a measured thundering-herd failure at 90s. Memory ceilings measured: POS 10.75 GB · pharmacy 11.5 GB · full 16.75 GB.

## Open decisions (from master §Appendix B — confirm as they arise)
1. First-cut scope: OMS correctness first (**chosen**) vs a parallel track first.
2. ~~Invoice-trigger defaults~~ **SETTLED 2026-08-11 (O7 §6 D-1): `ON_DISPATCH` for the distribution channel**,
   with stock reserved at CONFIRM. `ON_BOOKING` would make an amendment an edit to an issued invoice and a
   rejection a void; `ON_DELIVERY` is ruled out by the business process itself, since the delivery man carries
   the invoices. POS (`ON_SALE`) and storefront (`ON_PLACEMENT`) are unchanged.
3. Legacy storefront orders: `LEGACY_UNPOSTED` + leave the books (**O1 default**) vs back-post at original dates (touches closed periods).
4. `order-service` extraction at O6 (after correctness, before 2nd channel) — confirmed by sequence.
